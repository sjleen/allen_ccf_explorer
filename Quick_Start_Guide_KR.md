# Allen CCF Explorer — 빠른 시작 가이드

Allen Mouse Brain Common Coordinate Framework(**CCFv3, 2017**)의 3D mesh를 브라우저에서 열어, stereotaxic surgery에 바로 쓸 수 있는 좌표로 뇌 구조를 탐색하고 주입·삽입 지점을 설계하는 도구입니다. mesh 데이터가 앱 안에 내장되어 있어 **설치 및 인터넷 연결이 필요 없습니다.**

---

## 시작 전에

**좌표 규약**

- 단위는 **mm**, 원점은 **bregma**입니다.
- **ML+ = 오른쪽, AP+ = 앞쪽, DV+ = 등쪽(dorsal)**입니다.

**실행 환경**

- **Chrome · Edge**, 또는 **2023년 이후의 Firefox · Safari**가 필요합니다. mesh 압축 해제에 브라우저 내장 기능을 쓰기 때문에 오래된 브라우저에서는 열리지 않습니다.
- 처음 열면 mesh를 읽는 동안 **로딩 화면**이 몇 초 나타납니다. 이후에는 곧바로 뷰어가 열립니다.

**이 앱의 좌표계가 특별한 이유**

Allen CCFv3는 관류 고정된 뇌를 기준으로 만들어져서, 수술대 위의 flat skull 좌표와 그대로 맞지 않습니다. 이 앱은 기본으로 **각 축의 스케일과 뇌 전체 각도를 flat skull 위치에 맞춰 보정**합니다. 즉 화면에 보이는 좌표가 실제 stereotaxic 좌표에 가깝습니다.

> 보정을 끄고 원본 CCF 좌표를 보려면 **Coordinates** 탭의 **Preset**을 **Raw CCF**로, 직접 값을 조정하려면 **Custom**으로 바꾸면 됩니다.

---

## 화면 구성

| 영역         | 내용                                                                |
| ---------- | ----------------------------------------------------------------- |
| **왼쪽 패널**  | **Ontology**(구조 목록) · **Mesh Source** · **Coordinates**(좌표계 보정) 탭 |
| **중앙 뷰포트** | 위쪽에 **3D 뷰**, 아래쪽에 **Coronal · Sagittal · Horizontal** 2D 단면 뷰    |
| **오른쪽 패널** | **Points**(지점 등록) · **3D View**(표시 옵션) · **Export images**(이미지 출력)       |

좌우 패널은 뷰포트 **좌상단 / 우상단 버튼**으로 접었다 펼 수 있습니다. 화면이 작은 경우 특히 유용합니다.

---

## 1. 처음 한 번: 내 bregma에 맞추기

**새로 쓰기 시작한다면 이 과정을 가장 먼저 하세요.** 실험자마다 기준으로 삼는 bregma 위치가 조금씩 다르므로, 한 번 보정해 두면 이후 모든 좌표의 정확도가 올라갑니다.

**Coordinates** 탭 → **Bregma / Lambda personalization** 섹션에서 조정합니다. 권장 절차는 이렇습니다.

1. AP 방향으로 좁게 존재하는 구조를 target으로 고릅니다. **anterior commissure**(ML = 정중선 ± 1 mm 부근)가 적당합니다.
2. 미량의 dye를 주입합니다. 예: Trypan Blue **2–5 nL**.
3. 사용한 좌표를 point로 등록해 둡니다. (등록 방법은 아래 3번)
4. Histology 결과와 등록한 point가 같은 위치에 오도록 **Bregma AP**와 **Bregma DV** 값을 조절합니다.
5. 최종 값을 기록하거나 현재 session을 저장해 **개인 프로파일**처럼 재사용합니다. (저장 방법은 아래 5번)

bregma–lambda 거리(**Bregma to lambda**, 기본 4.2 mm)도 같은 섹션에서 바꿀 수 있습니다.

---

## 2. 뷰 다루기

**마우스 조작**

|          | 회전 / 이동                   | 확대·축소 |
| -------- | ------------------------- | ----- |
| **3D 뷰** | 왼쪽 드래그 = 회전, 오른쪽 드래그 = 이동 | 휠     |
| **2D 뷰** | 왼쪽 드래그 = 이동               | 휠     |

**단면 위치 바꾸기**

각 2D 뷰 아래의 슬라이더로 단면 좌표를 옮깁니다. **드래그**하거나, 슬라이더 상에서 **휠**을 굴리거나, 숫자칸에 **값을 직접 입력**해도 됩니다.
**Ctrl을 누른 채 휠**을 굴리면 이동 단위가 0.1 → 0.01 mm로 바뀌어 **10배 정밀하게** 움직입니다.

**한 화면만 크게 보기**

각 2D 뷰 상단의 **⛶** 버튼(3D 뷰는 우측 하단의 **⛶**)을 누르면 그 뷰 하나가 가운데 영역 전체를 차지합니다. 최대화 상태에서 **⤡** 를 누르거나 **Esc**를 누르면 4분할로 돌아옵니다.

**구조 켜고 끄기**

왼쪽 **Ontology** 창에서 원하는 region을 켜고 끄거나, 검색창으로 이름·약어·ID를 찾습니다.

> **흐리게 표시된 항목**은 Allen CCFv3에 mesh가 없는 구조입니다. 목록의 약 1,300개 구조 중 실제로 mesh가 제공되는 것은 약 840개뿐이라, 특정 단계 이상 세분화된 하위 구조는 표시할 수 없고 토글도 비활성화됩니다. 앱의 한계가 아니라 원본 데이터셋의 범위 문제입니다.

---

## 3. Point 등록하기

오른쪽 **Points** 패널에서 관심 지점을 등록합니다.

- **New point**를 눌러 새 지점을 만들고, 이름 · 좌표 · **반지름**을 지정합니다. 반지름은 infusion 시 확산 범위를 가늠하는 용도입니다.
- **2D 뷰를 클릭해서 좌표 넣기** — 좌표 줄 오른쪽 끝의 **◎ PICK** 버튼을 누르면 2D 뷰에서 커서가 십자로 바뀝니다. 이 상태에서 2D 단면의 원하는 지점을 클릭하면 그 위치의 좌표가 자동으로 입력됩니다. **Esc**로 취소합니다.
- point를 **더블클릭**하면 2D 뷰가 그 지점으로 이동합니다.
- 좌표가 여러 개라면 **Import points (CSV)**로 한 번에 등록할 수 있습니다. 임의의 point를 등록한 후 **Export points (CSV)**를 실행하면, 입력 포맷을 확인할 수 있습니다.

---

## 4. Path 분석: 삽입 경로 확인하기

각 point의 **Path**를 체크하면 그 지점에 도달하는 경로가 표시되고 **Path analysis** 항목이 생깁니다. 이를 클릭하면 경로가 지나는 **모든 region의 목록**을 볼 수 있습니다.

- 좌표나 각도를 바꾸면 창의 내용이 **실시간으로** 갱신됩니다.
- **Tilt**와 **Azimuth**를 입력해 경사 삽입 경로를 시뮬레이션할 수 있습니다. 이때 표시되는 **manip 값**은, bregma를 ML=AP=DV=0으로 맞춘 상태에서 해당 지점에 도달하려면 manipulator를 어디로 조정해야 하는지를 알려줍니다.
- **Path analysis** 창 상단 버튼으로 **SVG**(그림용) 또는 **CSV**(분석용)로 내보낼 수 있습니다.

> **manipulator 설정 맞추기** — **Coordinates** 탭의 **Manipulator convention** 섹션에서 ML, AP, DV 축 방향과 tilt · azimuth의 **적용 순서**를 실제 장비의 물리적 구조에 맞게 지정할 수 있습니다. 기본값은 널리 쓰이는 stereotaxic arm 구성을 따릅니다.

> **그래프 읽는 법** — 가로 방향은 **ontology level**입니다(왼쪽이 상위, 오른쪽이 하위 구조이며 상단에 레벨 번호가 표시됩니다). CCF mesh 특성상 한 지점이 둘 이상의 region에 동시에 속할 수 있는데, 같은 레벨에서 여러 region이 같은 구간을 공유하면 그만큼 폭을 나눠 갖습니다 — 둘이면 절반, 셋이면 1/3입니다. 겹치지 않는 구간은 레벨 폭 전체를 씁니다.

---

## 5. 저장하고 공유하기

앱 왼쪽 최상단, 앱 이름 옆의 **Save session**을 누르면 현재 설정과 등록한 point가 `.json` 파일로 저장됩니다. **Load session**으로 다시 불러옵니다.

- **Save session** 바로 위 입력칸에 문구를 적어 두면 파일명 끝에 언더바로 이어 붙습니다(예: `ccf-session-…_mouse42.json`).
- 이 `.json`(또는 앱 html 파일)을 건네주면 같은 앱을 쓰는 다른 사람이 **동일한 상태를 그대로 재현**할 수 있습니다.

---

## 6. 뷰포트를 그림으로 내보내기

오른쪽 **Export images** 패널의 버튼 세 개를 사용합니다.

| 버튼           | 결과                 |
| ------------ | ------------------ |
| **3D → PNG** | 3D 뷰를 고해상도 PNG로 저장 |
| **2D → PNG** | 현재 2D 단면을 PNG로 저장  |
| **2D → SVG** | 2D 단면을 **벡터**로 저장  |

SVG는 Illustrator · Inkscape에서 선 굵기나 색을 자유롭게 편집할 수 있어 **논문 그림 작업에 적합**합니다.
2D 출력은 현재 화면 상태를 따릅니다 — 한 뷰를 **최대화한 상태면 그 뷰만**, 아니면 **세 단면을 나란히** 내보냅니다.

---

## 그 외

- **Mesh opacity** (**3D View** 패널) — mesh의 투명도를 조절합니다.
- 화면 좌측 하단에는 커서가 가리키는 **좌표**와 **region 이름**이 실시간으로 표시됩니다.
